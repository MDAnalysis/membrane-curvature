name: docs
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - MDAnalysis
  - ipython
  - MDAnalysisTests
  - sphinx<7
    # Pip-only installs
  - pip:
    - nbsphinx
    - ipywidgets
    - nglview
