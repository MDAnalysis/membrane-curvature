name: docs
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - MDAnalysis
  - ipython
  - MDAnalysisTests
  - mdanalysis-sphinx-theme>=1.3.0
    # Pip-only installs
  - pip:
    - nbsphinx
    - ipywidgets
    - nglview
