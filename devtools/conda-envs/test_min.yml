name: test_min
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - sphinx
  - numpy==1.16.0
     # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - MDAnalysis==2.0.0b0