name: test
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - math
  - numpy
  - MDAnalysis
  - pickle
  - itertools
  - sphinx

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

