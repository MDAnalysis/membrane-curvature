name: test
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - numpy==1.16.0 
  - sphinx
     # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - MDAnalysis==2.0.0b0
