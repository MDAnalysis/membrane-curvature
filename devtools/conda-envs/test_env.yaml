name: test
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - numpy
  - MDAnalysis
  - mdtraj # temporary; remove after refactoring
  - sphinx

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

