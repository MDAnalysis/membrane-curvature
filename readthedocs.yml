# readthedocs.yaml

version: 2

sphinx:
  configuration: docs/source/conf.py

build:
  os: ubuntu-22.04
  tools:
    python: "mambaforge-4.10"

python:
  install:
    - method: setuptools
      path: .

conda:
  environment: docs/requirements.yaml
